# Authentication Service

Owns login, signup, logout, password login, face login entrypoints, JWT cookie handling, and user identity APIs. It reuses the existing auth controller and models during the migration.

